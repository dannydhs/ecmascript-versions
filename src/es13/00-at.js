const array = ["one", "two", "three", "four", "five", "six"]

// acceder al ultimo elemento del array sin saber la longitud. (antes de la nueva caracteristica)
console.log(array[array.length - 1])

// with at
console.log(array.at(-1))